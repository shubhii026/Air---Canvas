# Air---Canvas
Air Canvas is a Python-based computer vision application that uses OpenCV and MediaPipe to track hand gestures in real time, allowing users to draw on a virtual canvas without using a keyboard or mouse.
